import { AlertdirectiveDirective } from './alertdirective.directive';

describe('AlertdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
